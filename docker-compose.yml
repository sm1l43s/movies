version: '3.8'
services:
  legacy_postgres:
    image: postgres:16.2-alpine
    environment:
      POSTGRES_DB: legacy_movies
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: root
    ports:
      - "5432:5432"